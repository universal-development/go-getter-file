# Test configuration 1 - part of multi-config test
version: 1
name: "config1"

config:
  retries: 3
  timeout: 30s

sources:
  - url: "https://raw.githubusercontent.com/hashicorp/go-getter/main/README.md"
    dest: "out1"
